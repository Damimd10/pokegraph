type BerryFirmnesses {
  id: Int
  name: String
  berries: [NamedAPIResource]
  names: [Name]
}

type Query {
  getAllBerryFirmness(offset: Int, limit: Int): [BerryFirmnesses]
  getBerryFirmness(id: String!): BerryFirmnesses
}
