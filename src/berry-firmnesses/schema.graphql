type BerryFirmnesses {
  id: Int
  name: String
  berries: [NamedAPIResource]
  names: [Name]
}

type Query {
  getAllBerryFirmness(offset: Int, limit: Int): APIResourceList
  getBerryFirmness(id: String!): BerryFirmnesses
}
